diff --git a/CMakeLists.txt b/CMakeLists.txt
index 76b4710..4718733 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -18,6 +18,25 @@ if(CMAKE_CUDA_COMPILER)
     enable_language(CUDA)
     message(STATUS "Found CUDA compiler: ${CMAKE_CUDA_COMPILER}")
     message(STATUS "Found CUDA compiler version: ${CMAKE_CUDA_COMPILER_VERSION}")
+    find_package(CUDAToolkit QUIET)
+    if(CUDAToolkit_FOUND)
+        message(STATUS "Found CUDAToolkit: ${CUDAToolkit_VERSION}")
+        # PyTorch's CMake still relies on legacy FindCUDA variables in parts of
+        # its CUDA detection path (notably CUDA_INCLUDE_DIRS for a try_run
+        # header-version probe). Mirror the modern toolkit result into those
+        # variables so torch's probe sees <cuda.h> without hardcoding paths.
+        set(_aix_cuda_root "")
+        if(DEFINED CUDAToolkit_ROOT_DIR AND NOT CUDAToolkit_ROOT_DIR STREQUAL "")
+            set(_aix_cuda_root "${CUDAToolkit_ROOT_DIR}")
+        elseif(DEFINED CMAKE_CUDA_COMPILER_TOOLKIT_ROOT AND NOT CMAKE_CUDA_COMPILER_TOOLKIT_ROOT STREQUAL "")
+            set(_aix_cuda_root "${CMAKE_CUDA_COMPILER_TOOLKIT_ROOT}")
+        endif()
+
+        if(NOT _aix_cuda_root STREQUAL "")
+            set(CUDA_TOOLKIT_ROOT_DIR "${_aix_cuda_root}" CACHE PATH "CUDA toolkit root for legacy FindCUDA consumers" FORCE)
+            set(CUDA_INCLUDE_DIRS "${_aix_cuda_root}/include" CACHE PATH "CUDA include directory for legacy FindCUDA consumers" FORCE)
+        endif()
+    endif()
 
     # determine individual MAJOR, MINOR, PATCH version of CUDA
     string(REPLACE "." ";" CUDA_VERSION_LIST "${CMAKE_CUDA_COMPILER_VERSION}")
@@ -101,9 +120,13 @@ endif()
 
 # creation of virtual environment
 find_package(Python3 COMPONENTS Interpreter REQUIRED)
-set(AIX_VENV_DIR "${CMAKE_BINARY_DIR}/extern/venv-py${Python3_VERSION_MAJOR}${Python3_VERSION_MINOR}${Python3_VERSION_PATCH}")
+if (NOT AIX_VENV_DIR)
+  set(AIX_VENV_DIR "${CMAKE_BINARY_DIR}/extern/venv-py${Python3_VERSION_MAJOR}${Python3_VERSION_MINOR}${Python3_VERSION_PATCH}")
+endif()
+message(STATUS "Using AIX_VENV_DIR: ${AIX_VENV_DIR}")
+
 # check if the environment already exists
-if(NOT EXISTS "${AIX_VENV_DIR}/bin/activate")
+if(NOT EXISTS "${AIX_VENV_DIR}/bin/activate" AND NOT AIX_SKIP_VENV_CREATION)
   message(STATUS "No existing Python virtual environment found! Installing now!")
   execute_process(
     COMMAND ${Python3_EXECUTABLE} -m venv ${AIX_VENV_DIR}
@@ -211,8 +234,38 @@ if(WITH_TORCH)
   endif()
 
   if(NOT TORCH_CMAKE_PREFIX_CANDIDATE STREQUAL "")
+    set(_aix_saved_cpath "$ENV{CPATH}")
+    set(_aix_saved_ld_library_path "$ENV{LD_LIBRARY_PATH}")
+    if(CMAKE_CUDA_COMPILER)
+      set(_aix_cuda_include_dir "")
+      set(_aix_cuda_lib_dir "")
+      if(DEFINED CUDA_INCLUDE_DIRS AND NOT CUDA_INCLUDE_DIRS STREQUAL "")
+        list(GET CUDA_INCLUDE_DIRS 0 _aix_cuda_include_dir)
+      elseif(DEFINED CUDA_TOOLKIT_ROOT_DIR AND NOT CUDA_TOOLKIT_ROOT_DIR STREQUAL "")
+        set(_aix_cuda_include_dir "${CUDA_TOOLKIT_ROOT_DIR}/include")
+      endif()
+      if(DEFINED CUDA_TOOLKIT_ROOT_DIR AND NOT CUDA_TOOLKIT_ROOT_DIR STREQUAL "")
+        set(_aix_cuda_lib_dir "${CUDA_TOOLKIT_ROOT_DIR}/lib64")
+      endif()
+      if(NOT _aix_cuda_include_dir STREQUAL "")
+        if(_aix_saved_cpath STREQUAL "")
+          set(ENV{CPATH} "${_aix_cuda_include_dir}")
+        else()
+          set(ENV{CPATH} "${_aix_cuda_include_dir}:$ENV{CPATH}")
+        endif()
+      endif()
+      if(NOT _aix_cuda_lib_dir STREQUAL "")
+        if(_aix_saved_ld_library_path STREQUAL "")
+          set(ENV{LD_LIBRARY_PATH} "${_aix_cuda_lib_dir}")
+        else()
+          set(ENV{LD_LIBRARY_PATH} "${_aix_cuda_lib_dir}:$ENV{LD_LIBRARY_PATH}")
+        endif()
+      endif()
+    endif()
     list(APPEND CMAKE_PREFIX_PATH ${TORCH_CMAKE_PREFIX_CANDIDATE})
     find_package(Torch QUIET)
+    set(ENV{CPATH} "${_aix_saved_cpath}")
+    set(ENV{LD_LIBRARY_PATH} "${_aix_saved_ld_library_path}")
   endif()
 
   if(NOT Torch_FOUND)
@@ -272,6 +325,40 @@ if(WITH_TORCH)
     FILES ${TORCH_INSTALL_LIBS}
     DESTINATION ${CMAKE_INSTALL_PREFIX}/lib
   )
+
+  if(CMAKE_CUDA_COMPILER)
+    if(TARGET CUDA::cupti)
+      message(STATUS "Linking AIxeleratorService against CUDA::cupti")
+      set(AIX_CUPTI_LIB CUDA::cupti)
+    else()
+      set(_cupti_hints "")
+      if(DEFINED CUDAToolkit_LIBRARY_ROOT)
+        list(APPEND _cupti_hints ${CUDAToolkit_LIBRARY_ROOT})
+      endif()
+      if(DEFINED CUDAToolkit_ROOT)
+        list(APPEND _cupti_hints ${CUDAToolkit_ROOT})
+        list(APPEND _cupti_hints ${CUDAToolkit_ROOT}/extras/CUPTI/lib64)
+        list(APPEND _cupti_hints ${CUDAToolkit_ROOT}/lib64)
+      endif()
+      if(DEFINED CMAKE_CUDA_COMPILER_TOOLKIT_ROOT)
+        list(APPEND _cupti_hints ${CMAKE_CUDA_COMPILER_TOOLKIT_ROOT})
+        list(APPEND _cupti_hints ${CMAKE_CUDA_COMPILER_TOOLKIT_ROOT}/extras/CUPTI/lib64)
+        list(APPEND _cupti_hints ${CMAKE_CUDA_COMPILER_TOOLKIT_ROOT}/lib64)
+      endif()
+
+      find_library(AIX_CUPTI_LIB
+        NAMES cupti
+        HINTS ${_cupti_hints}
+        PATH_SUFFIXES extras/CUPTI/lib64 lib64
+      )
+    endif()
+
+    if(AIX_CUPTI_LIB)
+      message(STATUS "Using CUPTI library: ${AIX_CUPTI_LIB}")
+    else()
+      message(FATAL_ERROR "WITH_TORCH is ON with CUDA available, but the CUPTI library could not be found.")
+    endif()
+  endif()
 endif()
 
 if(WITH_TENSORFLOW)
@@ -392,4 +479,3 @@ add_subdirectory(models)
 if(BUILD_TESTS)
   add_subdirectory(test)
 endif()
-
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 4d63c4a..6fe8984 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -52,6 +52,9 @@ if(WITH_TORCH)
     target_sources(AIxeleratorServiceLib PRIVATE inferenceStrategy/torchInference/torchInference.cpp)
     target_include_directories(AIxeleratorServiceLib PUBLIC ${TORCH_INCLUDE_DIRS})
     target_link_libraries(AIxeleratorServiceLib PUBLIC ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(AIxeleratorServiceLib PUBLIC ${AIX_CUPTI_LIB})
+    endif()
 endif()
 
 if(WITH_TENSORFLOW)
@@ -112,4 +115,3 @@ install(
     LIBRARY DESTINATION ${CMAKE_INSTALL_PREFIX}/lib
     #PUBLIC_HEADER DESTINATION ${CMAKE_BINARY_DIR}/include
 )
-
diff --git a/test/CMakeLists.txt b/test/CMakeLists.txt
index f453ba1..8afc953 100644
--- a/test/CMakeLists.txt
+++ b/test/CMakeLists.txt
@@ -13,6 +13,9 @@ if(WITH_TORCH)
         target_include_directories(${UNITTEST_NAME}.x PRIVATE ${PROJECT_SOURCE_DIR}/src)
         target_include_directories(${UNITTEST_NAME}.x PRIVATE ${TORCH_INCLUDE_DIRS})
         target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${TORCH_LIBRARIES})
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
         install(
             TARGETS ${UNITTEST_NAME}.x
             RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin    
@@ -245,6 +248,9 @@ foreach(TEST ${AIXELERATOR_TESTS})
         target_sources(${UNITTEST_NAME}.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/torchInference/torchInference.cpp)
         target_include_directories(${UNITTEST_NAME}.x PRIVATE ${TORCH_INCLUDE_DIRS})
         target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${TORCH_LIBRARIES})
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
     endif()
     if(WITH_TENSORFLOW)
         target_sources(${UNITTEST_NAME}.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/tensorflowInference/tensorflowInference.cpp)
@@ -305,6 +311,9 @@ foreach(TEST ${AIXELERATOR_TESTS_HYBRID})
         target_sources(${UNITTEST_NAME}.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/torchInference/torchInference.cpp)
         target_include_directories(${UNITTEST_NAME}.x PRIVATE ${TORCH_INCLUDE_DIRS})
         target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${TORCH_LIBRARIES})
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
     endif()
     if(WITH_TENSORFLOW)
         target_sources(${UNITTEST_NAME}.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/tensorflowInference/tensorflowInference.cpp)
@@ -345,6 +354,12 @@ target_link_libraries(testAIxeleratorLib.x PRIVATE ${MPI_LIBRARIES})
 if(WITH_TORCH)
     target_include_directories(${UNITTEST_NAME}.x PRIVATE ${TORCH_INCLUDE_DIRS})
     target_link_libraries(testAIxeleratorLib.x PRIVATE ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(testAIxeleratorLib.x PRIVATE ${AIX_CUPTI_LIB})
+    endif()
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
 endif()
 if(WITH_TENSORFLOW)
     # target_include_directories(${UNITTEST_NAME}.x PRIVATE ${TENSORFLOW_INCLUDE_DIRS})
@@ -385,6 +400,12 @@ if(WITH_TORCH)
     target_include_directories(testTorchInference_interfaceC.x PRIVATE ${PROJECT_SOURCE_DIR}/src)
     target_include_directories(testTorchInference_interfaceC.x PRIVATE ${TORCH_INCLUDE_DIRS})
     target_link_libraries(testTorchInference_interfaceC.x PRIVATE ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(testTorchInference_interfaceC.x PRIVATE ${AIX_CUPTI_LIB})
+    endif()
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
     install(
         TARGETS testTorchInference_interfaceC.x
         RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin    
@@ -406,6 +427,12 @@ if(WITH_TORCH)
     target_include_directories(testTorchInference_interfaceF.x PRIVATE ${PROJECT_SOURCE_DIR}/src)
     target_include_directories(testTorchInference_interfaceF.x PRIVATE ${TORCH_INCLUDE_DIRS})
     target_link_libraries(testTorchInference_interfaceF.x PRIVATE ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(testTorchInference_interfaceF.x PRIVATE ${AIX_CUPTI_LIB})
+    endif()
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
     set_target_properties(testTorchInference_interfaceF.x PROPERTIES LINKER_LANGUAGE Fortran)
     install(
         TARGETS testTorchInference_interfaceF.x
@@ -431,6 +458,12 @@ if(WITH_TORCH)
     target_sources(testAIxeleratorService_interfaceC.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/torchInference/torchInference.cpp )
     target_include_directories(testAIxeleratorService_interfaceC.x PRIVATE ${TORCH_INCLUDE_DIRS})
     target_link_libraries(testAIxeleratorService_interfaceC.x PRIVATE ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(testAIxeleratorService_interfaceC.x PRIVATE ${AIX_CUPTI_LIB})
+    endif()
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
 endif()
 if(WITH_TENSORFLOW)
     target_sources(testAIxeleratorService_interfaceC.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/tensorflowInference/tensorflowInference.cpp)
@@ -481,6 +514,12 @@ if(WITH_TORCH)
     target_sources(testAIxeleratorService_interfaceF.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/torchInference/torchInference.cpp )
     target_include_directories(testAIxeleratorService_interfaceF.x PRIVATE ${TORCH_INCLUDE_DIRS})
     target_link_libraries(testAIxeleratorService_interfaceF.x PRIVATE ${TORCH_LIBRARIES})
+    if(AIX_CUPTI_LIB)
+        target_link_libraries(testAIxeleratorService_interfaceF.x PRIVATE ${AIX_CUPTI_LIB})
+    endif()
+        if(AIX_CUPTI_LIB)
+            target_link_libraries(${UNITTEST_NAME}.x PRIVATE ${AIX_CUPTI_LIB})
+        endif()
 endif()
 if(WITH_TENSORFLOW)
     target_sources(testAIxeleratorService_interfaceF.x PRIVATE ${PROJECT_SOURCE_DIR}/src/inferenceStrategy/tensorflowInference/tensorflowInference.cpp)
